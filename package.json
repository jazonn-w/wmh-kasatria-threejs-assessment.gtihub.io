{
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"deploy": "gh-pages -d dist"
	},
	"dependencies": {
		"three": "^0.155.0"
	},
	"devDependencies": {
		"gh-pages": "^6.0.0",
		"vite": "^4.4.9"
	}
}
